{
  "name": "chrome-killer-cloud",
  "version": "1.0.0",
  "private": true,
  "description": "Optional sync + auth API for PySearch / Chrome Killer (run separately from Electron)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "uuid": "^10.0.0"
  }
}
